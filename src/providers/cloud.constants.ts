export const cloudDefaults = Object.freeze({
  deadlineMs: 600_000,
  retainBytes: 65_536,
  cancellationMs: 15_000,
});
export const cloudRoots = Object.freeze({
  vercel: "/vercel/sandbox/outpost",
  daytonaHome: "/home/daytona",
});
