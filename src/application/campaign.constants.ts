export const campaignDefaults = Object.freeze({
  cycles: 10,
  concurrency: 3,
  implementationPasses: 100,
  reviewPasses: 1,
  standards:
    "Follow the repository conventions. Keep changes focused, test behavior and commit the result.",
});
